# BackEnd

Instructions for setting up and running the backend go here.

- Add language/runtime (Python/Node/etc.) and version.
- Add dependency install commands.
- Add how to run tests and start the server.

Example
1. Create a venv and install dependencies.

   python -m venv .venv
   .venv\Scripts\activate
   pip install -r requirements.txt

2. Run the server (example):

   python app.py
