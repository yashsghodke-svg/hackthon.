# FrontEnd

Instructions for setting up and running the frontend go here.

- Specify framework (React/Vue/Angular) and required Node version.
- Install dependencies and run dev server.

Example
1. Install dependencies:

   npm install

2. Run dev server:

   npm start
